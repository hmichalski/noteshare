class User {
    constructor(id, username, email) {
      this.id = id;           // User ID (int)
      this.username = username; // Username
      this.email = email;     // Email address
    }
  }
  
  module.exports = User;
  